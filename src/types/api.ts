/**
 * @module
 * Barrel re-exporting the generated Orval API types from `@api`, so consumers import from
 * `@/types` rather than reaching into the generated package directly.
 */
export * from '@api';
