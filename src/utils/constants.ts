/**
 * Public base path used by the app router.
 */
export const PUBLIC_PATH = import.meta.env.VITE_APP_PUBLIC_PATH ?? '/';

/**
 * Shared fallback used when a display value is empty or unavailable.
 */
export const EMPTY_VALUE = '—';
